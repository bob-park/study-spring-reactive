## spring
spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${google.client-id}
            client-secret: ${google.client-secret}
            scope:
              - profile
              - email

## logging
logging:
  level:
    web: debug
    com.hwpark: debug
